{
  "name": "tv",
  "version": "0.0.0",
  "private": true,
  "description": "OTT TV app (Apple TV + Android TV / Fire TV). SCAFFOLD ONLY — not yet runnable.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'TV app: lint disabled in scaffold' && exit 0"
  },
  "dependencies": {
    "@ott/types": "workspace:*",
    "@ott/ui-tokens": "workspace:*"
  },
  "devDependencies": {
    "@ott/config": "workspace:*",
    "@types/node": "^22.10.5",
    "@types/react": "^19.0.7",
    "typescript": "^5.7.0"
  }
}
